# Multi-Venue Performance Report

**Period:** {{ period }}  
**Date Range:** {{ date_range[0] }} to {{ date_range[1] }}  
**Report Generated:** {{ "now"|strftime("%B %d, %Y at %I:%M %p") }}

---

## Period Overview

| Metric | Value |
|--------|-------|
| **Total Venues** | {{ venues_count }} |
| **Total Responses** | {{ total_responses }} |
| **Period Type** | {{ period_type|capitalize }} |

---

## Performance Summary

| Metric | Average |
|--------|---------|
| **NPS (Combined)** | {{ "%.1f"|format(metrics_avg.nps_avg) if metrics_avg.nps_avg else "N/A" }} / 10 |
| **Likelihood to Return (LTR)** | {{ "%.1f"|format(metrics_avg.ltr_avg) }} / 5 |
| **Fun** | {{ "%.1f"|format(metrics_avg.fun_avg) }} / 5 |
| **Helpful** | {{ "%.1f"|format(metrics_avg.helpful_avg) }} / 5 |
| **Issues Reported** | {{ "%.1f"|format(metrics_avg.issues_pct) }}% |
| **Issue Resolution** | {{ "%.1f"|format(metrics_avg.resolution_avg) if metrics_avg.resolution_avg else "N/A" }} / 5 |

---

## Venue Ranking

| Rank | Venue | NPS (Composite) | LTR | Fun | Resolution | Responses |
|------|-------|-----------------|-----|-----|------------|-----------|
{% for venue in venues_ranked %}
| {{ venue.rank }} | {{ venue.venue }} | {{ "%.2f"|format(venue.composite_score) if venue.composite_score else "N/A" }} | {{ "%.1f"|format(venue.ltr_avg) }} | {{ "%.1f"|format(venue.fun_avg) }} | {{ "%.1f"|format(venue.resolution_avg) if venue.resolution_avg else "N/A" }} | {{ venue.responses }} |
{% endfor %}

---

{% if comment_themes %}

## Key Comment Themes

{% for theme, count, venues in comment_themes %}
- **{{ theme }}** ({{ count }} mentions)
{% endfor %}

{% endif %}

{% if analysis.ai_available %}

## Period Analysis

{{ analysis.overview }}

{% else %}

## Analysis Status

> {{ analysis.unavailable_reason }}

{% endif %}

---

*This report aggregates performance metrics across all venues for the {{ period }} period, providing a comprehensive view of multi-venue performance and identifying top performers.*
